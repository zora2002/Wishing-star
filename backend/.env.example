# 本機開發用的設定，複製這個檔案改名為 .env 即可
PORT=4000
AWS_REGION=ap-east-2
TABLE_NAME=WishingStarWishes

# 有這個值時，程式會連到本機 Docker 裡的 DynamoDB
# 之後部署到 AWS Lambda 時，把這一行整個拿掉，就會改連正式的 DynamoDB
DYNAMODB_ENDPOINT=http://localhost:8000
